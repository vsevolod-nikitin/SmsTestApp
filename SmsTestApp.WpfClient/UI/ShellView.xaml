<Window x:Class="SmsTestApp.WpfClient.UI.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmsTestApp.WpfClient.UI"
        d:DataContext="{d:DesignInstance local:ShellViewModel}"
        mc:Ignorable="d"
        Title="Тестовое WPF-приложение для SmartMealService" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" UseLayoutRounding="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <local:RoundedRectClipConverter x:Key="RoundedClipConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Border BorderThickness="{StaticResource ShellBorderThickness}" CornerRadius="{StaticResource ShellBorderRadius}">
            <Border.Clip>
                <MultiBinding Converter="{StaticResource RoundedClipConverter}">
                    <Binding RelativeSource="{RelativeSource Self}" Path="ActualWidth"/>
                    <Binding RelativeSource="{RelativeSource Self}" Path="ActualHeight"/>
                    <Binding RelativeSource="{RelativeSource Self}" Path="CornerRadius"/>
                </MultiBinding>
            </Border.Clip>

            <DataGrid ItemsSource="{Binding Variables}" AutoGenerateColumns="False" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Поле" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Значение" Binding="{Binding Value}" Width="*"/>
                    <DataGridTextColumn Header="Комментарий" Binding="{Binding Description}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border BorderThickness="{StaticResource ShellBorderThickness}" CornerRadius="{StaticResource ShellBorderRadius}" BorderBrush="Gray"/>
    </Grid>
</Window>
